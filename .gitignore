# ───────── secrets — NEVER commit ─────────
**/.env*
!**/.env.example
!**/.env.production.example
*.key
*.pem
*.p12

# ───────── processing outputs (still excluded) ─────────
**/output/
**/results/
# ───────── employee PII — NEVER commit ─────────
# Real timesheet source docs (names + hours) belong in the private `ts-uploads`
# Supabase bucket, NOT git. Purge any already in history: deploy/purge-timesheet-pii.sh
Timesheet-April/
TimeSheet-May/
Timesheet/

# ───────── python ─────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# ───────── node / next.js ─────────
node_modules/
.next/
out/
*.tsbuildinfo

# ───────── os / editor / local tooling ─────────
.DS_Store
*.log
.claude/
.vscode/
.idea/

# generated evaluation reports (contain employee PII; regenerated per run)
/reports/

# runtime tunnel URL (written by the tunnel script; ephemeral)
.engine_tunnel_url

# stray archives / working copies
*.zip

# aws-cli deploy bookkeeping
deploy-aws-native/.aws-state*
*.pem
