# ───────── secrets — NEVER commit ─────────
**/.env
**/.env.local
**/.env.*.local
*.key
*.pem
*.p12

# ───────── processing outputs (still excluded) ─────────
**/output/
**/results/
# NOTE: Timesheet/ and TimeSheet-May/ source data are intentionally INCLUDED
# in this repo per the owner's decision (they contain employee PII + invoices).

# ───────── python ─────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# ───────── node / next.js ─────────
node_modules/
.next/
out/
*.tsbuildinfo

# ───────── os / editor / local tooling ─────────
.DS_Store
*.log
.claude/
.vscode/
.idea/
